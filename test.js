// const { getInstructionsByKeyword } = require('./src/controllers/instructions_controllers'); // Import the function

// const testGetInstructionsByKeyword = () => {
//   const inputString = 'this is a cut'; // Set the inputString you want to test

//   getInstructionsByKeyword(inputString)
//     .then((data) => {
//       if (data.length > 0) {
//         console.log('Instruction:', data[0].instruction); // Output the retrieved instruction to the console
//       } else {
//         console.log('No instruction found for the given keyword.');
//       }
//     })
//     .catch((error) => {
//       console.error('An error occurred:', error); // Output any errors to the console
//     });
// };

// testGetInstructionsByKeyword();
